server {
    listen 80;
    root /app;

    location / {
        index index.php;
    }

    location ~ \.php$ {
        fastcgi_pass script:9000;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME /app$fastcgi_script_name;
    }
}

